{
  "name": "viz-components",
  "version": "0.0.31",
  "scripts": {
    "prepare": "npm run co:login",
    "co:login": "aws codeartifact login --tool npm --repository vizcolib --domain frontend",
    "build": "tsc -p tsconfig.schematics.json",
    "postbuild": "node build.js"
  },
  "files": [
    "*",
    "raw-html/*"
  ],
  "schematics": "./schematics/collection.json",
  "peerDependencies": {
    "@angular/common": "^14.0.0",
    "@angular/core": "^14.0.0"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  }
}
