{
  "name": "@web-ast/viz-components",
  "version": "0.0.3",
  "scripts": {
    "prepare": "npm run co:login",
    "co:login": "aws codeartifact login --tool npm --repository vizcolib --domain frontend --namespace @web-ast",
    "build": "tsc -p tsconfig.schematics.json",
    "postbuild": "node build.js"
  },
  "files": [
    "*",
    "raw-html/*"
  ],
  "schematics": "./schematics/collection.json",
  "peerDependencies": {
    "@angular/common": "^14.0.0",
    "@angular/core": "^14.0.0",
    "d3": "^7.6.1"
  },
  "dependencies": {
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/common": "^14.0.0",
    "@angular/core": "^14.0.0",
    "d3": "^7.4.4"
  }
}
